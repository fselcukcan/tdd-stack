{
  "name": "tdd-basic-stack",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage",
    "test:watch": "jest --watchAll --verbose",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.5.1",
    "vite": "^2.8.0"
  }
}
